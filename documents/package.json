{
  "name": "@refinitiv-ui/docs",
  "version": "6.5.7",
  "private": true,
  "description": "Element Framework Documentation",
  "engines": {
    "node": ">=16.11.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "start:dev": "concurrently \"pandora src dist --watch\" \"serve dist -s\"",
    "start": "serve dist -s",
    "copy:resources": "cp src/_redirects dist/_redirects && cp -r resources dist/resources && cp src/theme-loader.js dist/theme-loader.js",
    "prebuild": "rm -rf build && nx run @refinitiv-ui/elements:api-analyzer && node ./scripts/element.injector.js",
    "build": "pandora build dist --clean && npm run copy:resources && npm run bundle",
    "bundle": "node esbuild.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Refinitiv/refinitiv-ui.git",
    "directory": "documents"
  },
  "keywords": [
    "Element Framework",
    "EF"
  ],
  "author": "LSEG",
  "license": "Apache-2.0",
  "devDependencies": {
    "esbuild": "^0.18.1",
    "serve": "^14.2.0",
    "pandora-book": "file:tools/pandora-book-3.0.0-pride.1.tgz"
  },
  "dependencies": {
    "@refinitiv-ui/elements": "^6.8.5",
    "@refinitiv-ui/halo-theme": "^6.5.3",
    "fast-glob": "^3.2.12"
  }
}
