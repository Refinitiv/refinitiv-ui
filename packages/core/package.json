{
  "name": "@refinitiv-ui/core",
  "version": "5.0.0-alpha.1",
  "description": "Element Framework Core",
  "main": "./lib/elf.js",
  "module": "./lib/elf.js",
  "types": "./lib/elf.d.ts",
  "repository": {
    "type": "git",
    "url": "git@git.sami.int.thomsonreuters.com:elf/refinitiv-ui.git",
    "directory": "packages/core"
  },
  "author": "Refinitiv",
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch --preserveWatchOutput --sourceMap",
    "lint": "eslint . --fix",
    "start": "concurrently \"yarn build:watch\" \"es-dev-server --config ../../server.config.js\"",
    "test": "yarn build && karma start ../../karma.conf.js",
    "test:snapshots": "yarn build && yarn __test__ --update-snapshots --prune-snapshots",
    "test:watch": "yarn build && yarn __test__ --auto-watch=true --single-run=false",
    "prepublishOnly": "node ../../released-tools/released.js"
  },
  "dependencies": {
    "@juggle/resize-observer": "^3.3.0",
    "lit-element": "^2.4.0",
    "lit-html": "^1.3.0"
  },
  "devDependencies": {
    "@refinitiv-ui/test-helpers": "^5.0.0-alpha.1"
  }
}
