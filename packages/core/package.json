{
  "name": "@refinitiv-ui/core",
  "version": "5.0.0-beta.1",
  "description": "Element Framework Core",
  "main": "./lib/elf.js",
  "module": "./lib/elf.js",
  "types": "./lib/elf.d.ts",
  "repository": {
    "type": "git",
    "url": "git@git.sami.int.thomsonreuters.com:elf/refinitiv-ui.git",
    "directory": "packages/core"
  },
  "author": "Refinitiv",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch --preserveWatchOutput --sourceMap",
    "lint": "eslint . --fix",
    "test": "yarn build && karma start ../../karma.conf.js",
    "test:snapshots": "yarn build && yarn test --update-snapshots --prune-snapshots",
    "test:watch": "yarn build && yarn test --auto-watch=true --single-run=false"
  },
  "dependencies": {
    "@juggle/resize-observer": "^3.3.0",
    "lit-element": "^2.4.0",
    "lit-html": "^1.3.0"
  },
  "devDependencies": {
    "@refinitiv-ui/test-helpers": "^5.0.0-beta.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
