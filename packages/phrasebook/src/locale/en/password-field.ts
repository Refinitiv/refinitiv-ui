import { Phrasebook } from '../../translation.js';

const translations = {
  SHOW_PASSWORD: 'Show password',
  HIDE_PASSWORD: 'Hide password'
};

Phrasebook.define('en', 'ui-sub-password-field', translations);

export default translations;
