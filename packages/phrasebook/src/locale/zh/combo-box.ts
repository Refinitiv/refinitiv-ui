import { Phrasebook } from '../../';
import './shared';

const translations = {
  MULTIPLE_ITEMS: '多项',
  NO_OPTIONS: '没有选择'
};

Phrasebook.define('zh', 'ef-combo-box', translations);

export default translations;
