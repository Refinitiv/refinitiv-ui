import { Phrasebook } from '../../';
import './shared';

const translations = {
  MULTIPLE_ITEMS: '多项',
  NO_OPTIONS: '未找到结果。'
};

Phrasebook.define('zh', 'ef-combo-box', translations);

export default translations;
