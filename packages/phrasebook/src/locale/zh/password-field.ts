import { Phrasebook } from '../../translation.js';

const translations = {
  SHOW_PASSWORD: '显示密码',
  HIDE_PASSWORD: '隐藏密码'
};

Phrasebook.define('zh', 'ds-sub-password-field', translations);

export default translations;
