{
  "name": "@refinitiv-ui/tokens",
  "version": "7.0.0-next.0",
  "author": "LSEG",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf lib && toko -c toko.config.js -d lib",
    "build:watch": "chokidar '**/*.json' --command \"npm run build\" --debounce=500",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "toko": "^0.1.0"
  }
}
