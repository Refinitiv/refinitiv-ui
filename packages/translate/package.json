{
  "name": "@refinitiv-ui/translate",
  "version": "5.0.0-alpha.1",
  "description": "i18n implementation for Element Framework components",
  "main": "./lib/translate.js",
  "module": "./lib/translate.js",
  "types": "./lib/translate.d.ts",
  "repository": {
    "type": "git",
    "url": "git@git.sami.int.thomsonreuters.com:elf/refinitiv-ui.git",
    "directory": "packages/translate"
  },
  "author": "Refinitiv",
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "tsc --sourceMap false",
    "build:watch": "tsc --watch --preserveWatchOutput --sourceMap",
    "lint": "eslint . --fix",
    "start": "concurrently \"yarn build:watch\" \"web-dev-server --config ../../server.config.js\"",
    "test": "yarn build && karma start ../../karma.conf.js",
    "test:watch": "yarn build && yarn test --auto-watch=true --single-run=false"
  },
  "dependencies": {
    "@refinitiv-ui/core": "^5.0.0-alpha.1",
    "@refinitiv-ui/phrasebook": "^5.0.0-alpha.1",
    "@refinitiv-ui/i18n": "^5.0.0-alpha.1",
    "lit-html": "^1.3.0"
  },
  "organisationConfig": {
    "public": true
  }
}
