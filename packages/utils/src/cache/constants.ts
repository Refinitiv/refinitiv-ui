/*
 * Default cache storage name prefix
 */
export enum DatabasePrefix {
  DEFAULT = 'ef'
}

export const CACHE_PREFIX = DatabasePrefix.DEFAULT;
export const MESSENGER_PREFIX = DatabasePrefix.DEFAULT;
