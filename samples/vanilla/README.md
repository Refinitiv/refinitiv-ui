# Vanilla + Refinitiv-ui

This project was generated without the use of framework.

## Getting started

Install dependencies and deduplicate packages.

```sh
  npm install && npm dedupe
```

Serve application in development mode.

```sh
  npm start
```
